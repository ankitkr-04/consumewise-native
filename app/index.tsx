import { Text, View } from "react-native";

const index = () => {
  return (
    <View className="flex h-screen items-center justify-center">
      <Text className="text-lg font-semibold text-red-400">Hello</Text>
    </View>
  );
};
export default index;
